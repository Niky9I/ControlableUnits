using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.AI;

public class UnitComponent : MonoBehaviour
{
	private void Awake()
	{
		//if (gameObject.TryGetComponent<WayPointComponent>(out var wayPointComponent) && gameObject.TryGetComponent<SpeedComponent>(out var speed))
		//{
		//	if (gameObject.TryGetComponent<NavMeshAgent>(out var agent))
		//	{
		//		agent.destination = wayPointComponent.EndPoint.position;
		//		agent.speed = speed.CurrentSpeed;
		//		agent.enabled = true;
		//	}
		//}
	}
	void Update()
    {
		
	}
}
